# Non-secret defaults only.
# Production secrets MUST be supplied through environment variables.

server.port=8080
database.url=jdbc:postgresql://localhost:5432/secureline_db
database.username=secureline
redis.url=redis://localhost:6379

# Required environment variables:
# SECURELINE_DATABASE_PASSWORD
# SECURELINE_ZK_SECRET
